{
  "name": "bc-rolodex",
  "version": "0.0.2",
  "description": "BigCommerce Test Tool",
  "main": "src/main.js",
  "engines": {
    "node": ">=4.2.4"
  },
  "bin": {
    "rolodex": "src/main.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "rm -rf dist && mkdir dist && babel -d dist/ src/"
  },
  "author": "Mick Ryan",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/mickr/bc-rolodex.git"
  },
  "dependencies": {
    "axios": "^0.9.1",
    "bluebird": "^3.3.5",
    "chance": "^1.0.2",
    "colors": "^1.1.2",
    "commander": "^2.9.0"
  },
  "devDependencies": {
    "babel-cli": "^6.7.5",
    "babel-preset-es2015": "^6.6.0"
  }
}
